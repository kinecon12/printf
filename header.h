#ifndef HEADER_H
#define HEADER_H

/* Standard C library for header files */
#include <stdarg.h>
#include <unistd.h>

void getc_ints(int num);
int putchers(char n);
int putt_intts(int integer);
int  p_printf(const char *format, ...);
int putts(char *string);
int m_put(char *string);

#endif /* HEADER.H file */
